// History content bank, keyed by level id (sec2 only for these kids). Same schema as Chinese,
// plus optional `source` field for source-based inquiry questions: { type:'source', context, q, answer, explanation }
const CONTENT_HISTORY = {
  sec2: []
};
